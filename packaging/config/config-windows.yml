# Blockasaurus default configuration (Windows)
# See https://github.com/chrissnell/blockasaurus for full documentation

# SQLite database for upstream, blocking, and custom DNS configuration
databasePath: blockasaurus.db

ports:
  dns: 53
  http: 4000

log:
  level: info
